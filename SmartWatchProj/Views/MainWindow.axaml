<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:SmartWatchProj.ViewModels"
        xmlns:models="using:SmartWatchProj.Models"
        xmlns:devices="using:SmartWatchProj.Models.Devices"
        xmlns:diag="using:SmartWatchProj.Models.Diagnostics"
        xmlns:conv="using:SmartWatchProj.Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="700"
        x:Class="SmartWatchProj.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="SmartWatchProj"
        FontFamily="Inter, DejaVu Sans, Arial"
        UseLayoutRounding="True"
        MinWidth="1000" MinHeight="600"
        WindowState="FullScreen">

    <!-- Background -->
	<Window.Background>
		<LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
			<GradientStop Offset="0" Color="#FF6D00" />
			<GradientStop Offset="1" Color="#FFCC40" />
		</LinearGradientBrush>
	</Window.Background>

	<!-- Resources -->
	<Window.Resources>
		<conv:RecommendationToBrushConverter x:Key="RecToBrush" />
	</Window.Resources>
	
	<!-- Styles (без изменений) -->
	<Window.Styles>
		<Style Selector="Button">
			<Setter Property="Background" Value="#FF6D00" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="CornerRadius" Value="8" />
			<Setter Property="Padding" Value="12,6" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="Transitions">
				<Transitions>
					<BrushTransition Property="Background" Duration="0:0:0.3" />
				</Transitions>
			</Setter>
		</Style>

		<Style Selector="Button:pointerover /template/ ContentPresenter">
			<Setter Property="Background" Value="#E64517" />
		</Style>

		<!-- ProgressBar: фирменный оранжевый (как кнопки) -->
		<Style Selector="ProgressBar">
			<Setter Property="Background" Value="#FFF3E0" />
			<Setter Property="Foreground" Value="#FF6D00" />
		</Style>

		<!-- Полоса заполнения (determinate) -->
		<Style Selector="ProgressBar /template/ Border#PART_Indicator">
			<Setter Property="Background" Value="#FF6D00" />
		</Style>
		<Style Selector="ProgressBar /template/ Rectangle#PART_Indicator">
			<Setter Property="Fill" Value="#FF6D00" />
		</Style>

		<!-- Полоса "бегущая" (indeterminate) -->
		<Style Selector="ProgressBar /template/ Border#PART_IndeterminateIndicator">
			<Setter Property="Background" Value="#FF6D00" />
		</Style>
		<Style Selector="ProgressBar /template/ Rectangle#PART_IndeterminateIndicator">
			<Setter Property="Fill" Value="#FF6D00" />
		</Style>

		<!-- Убираем синий фокус (рамку с углами) у элементов истории -->
		<Style Selector="ListBox.history ListBoxItem:focus">
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="BorderThickness" Value="0" />
		</Style>

		<!-- На всякий случай: если тема рисует фокус внутри шаблона -->
		<Style Selector="ListBox.history ListBoxItem:focus /template/ Border">
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="BorderThickness" Value="0" />
		</Style>

		<Style Selector="ProgressBar">
			<!-- Переопределяем accent-кисти ТОЛЬКО внутри ProgressBar -->
			<Style.Resources>
				<SolidColorBrush x:Key="SystemControlHighlightAccentBrush" Color="#FF6D00"/>
				<SolidColorBrush x:Key="SystemControlHighlightAccentBrush2" Color="#FF6D00"/>
				<SolidColorBrush x:Key="SystemControlHighlightAccentBrush3" Color="#FF6D00"/>
				<SolidColorBrush x:Key="SystemAccentColorBrush" Color="#FF6D00"/>
			</Style.Resources>

			<Setter Property="Background" Value="#FFF3E0" />
			<Setter Property="Foreground" Value="#FF6D00" />
		</Style>
	</Window.Styles>
    <Grid>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<!-- Заголовок -->
			<RowDefinition Height="*" />
			<!-- Камера/история -->
			<RowDefinition Height="Auto" />
			<!-- Рекомендации -->
			<RowDefinition Height="Auto" />
			<!-- Плитки -->
			<RowDefinition Height="Auto" />
			<!-- Статус/выход -->
		</Grid.RowDefinitions>

        <!-- Заголовок с иконкой -->
		<StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
			<Image Source="/Assets/rosgaz-logo.png" Width="64" Height="64" Margin="0,0,10,0" />
			<TextBlock Text="Мониторинг здоровья сотрудников" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="Black" />
		</StackPanel>

		<!-- Левая панель: Ввод и управление (ровная колонка + сообщения) -->
		<Border Grid.Row="0"
				Grid.Column="2"
				IsVisible="{Binding !IsDevicePanelOpen}"
				Background="#FFF8F2"
				CornerRadius="16"
				Padding="14,12"
				BorderBrush="#22000000"
				BorderThickness="1"
				BoxShadow="0 8 20 0 #14000000"
				HorizontalAlignment="Right"
				MaxWidth="360">
			<Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
				<StackPanel Spacing="4">
					<TextBlock Text="{Binding DeviceModeLabel}"
							   Foreground="{Binding DeviceModeBrush}"
							   FontWeight="SemiBold"
							   FontSize="13"/>
					<TextBlock Text="{Binding DevicePanelSummary}"
							   FontSize="12"
							   TextWrapping="Wrap"
							   Foreground="#1E1E1E"/>
				</StackPanel>

				<Button Grid.Column="1"
						Content="{Binding DevicePanelToggleText}"
						Command="{Binding ToggleDevicePanelCommand}"
						HorizontalAlignment="Right"
						VerticalAlignment="Center"
						Padding="14,8"/>
			</Grid>
		</Border>

		<StackPanel Grid.Row="1" Grid.Column="0"
					Spacing="14"
					Margin="0,20,20,0"
					VerticalAlignment="Top"
					HorizontalAlignment="Center"
					Width="300">

			<TextBlock Text="Ввод ID карты"
					   FontSize="18"
					   FontWeight="SemiBold"
					   TextAlignment="Center"
					   HorizontalAlignment="Center"
					   Margin="0,0,0,6"/>

			<TextBox Text="{Binding CardId}"
					 Watermark="ID карты"
					 Width="260"
					 FontSize="14"
					 HorizontalAlignment="Center"/>

			<TextBox Watermark="ФИО сотрудника"
					 Text="{Binding NewEmployeeName, UpdateSourceTrigger=PropertyChanged}"
					 Width="260"
					 HorizontalAlignment="Center"/>

			<TextBox Watermark="CardId"
					 Text="{Binding NewCardId, UpdateSourceTrigger=PropertyChanged}"
					 Width="260"
					 HorizontalAlignment="Center"/>

			<Button Content="Добавить сотрудника"
					Command="{Binding AddEmployeeCommand}"
					Width="260"
					HorizontalAlignment="Center"
					HorizontalContentAlignment="Center"/>

			<Button Content="Симулировать пропуск"
					Command="{Binding SimulateCardReadCommand}"
					IsEnabled="{Binding IsSimulationEnabled}"
					Width="260"
					HorizontalAlignment="Center"
					HorizontalContentAlignment="Center"/>

			<Button Content="Старт"
					Command="{Binding StartCommand}"
					IsEnabled="{Binding CanStartWorkflow}"
					Width="260"
					HorizontalAlignment="Center"
					HorizontalContentAlignment="Center"/>

			<TextBlock Text="{Binding StartAvailabilityReason}"
					   IsVisible="False"
					   Width="260"
					   TextWrapping="Wrap"
					   FontSize="12"
					   Foreground="#7A1F1F"
					   TextAlignment="Center"/>

			<Button Content="Экстренная остановка"
					Command="{Binding EmergencyStopCommand}"
					IsEnabled="{Binding CanEmergencyStop}"
					Width="260"
					HorizontalAlignment="Center"
					HorizontalContentAlignment="Center"
					Background="#C62828"/>

			<ProgressBar IsIndeterminate="{Binding IsCollectingData}"
						 IsVisible="False"
						 Height="18"
						 Width="260"
						 Margin="0,8,0,0"
						 HorizontalAlignment="Center"/>

			<TextBlock Text="{Binding HardwareSafetyStatus}"
					   IsVisible="False"
					   Width="260"
					   TextWrapping="Wrap"
					   FontSize="12"
					   FontWeight="SemiBold"
					   Foreground="#7A1F1F"
					   TextAlignment="Center"/>

			<!-- ВЕРХНЯЯ панель: только ошибки от кнопок/ввода -->
			<TextBlock Text="{Binding CameraModeStatusText}"
					   IsVisible="False"
					   Width="260"
					   TextWrapping="Wrap"
					   FontSize="12"
					   Foreground="#7A1F1F"
					   TextAlignment="Center"/>

			<Border IsVisible="False"
					Background="#FFFFFF"
					CornerRadius="14"
					BorderBrush="#22000000"
					BorderThickness="1"
					BoxShadow="0 8 20 0 #1A000000"
					Width="260"
					Margin="0,10,0,0">

				<Grid ColumnDefinitions="8,*">
					<Border Grid.Column="0"
							Background="{Binding TopInfoColor}"
							CornerRadius="14,0,0,14"/>
					<Border Grid.Column="1" Padding="12,10">
						<TextBlock Text="{Binding TopInfoMessage}"
								   Foreground="{Binding TopInfoColor}"
								   FontSize="13"
								   FontWeight="Bold"
								   TextWrapping="Wrap"/>
					</Border>
				</Grid>
			</Border>

			<Border Background="#FFF8F2"
					IsVisible="False"
					CornerRadius="14"
					Padding="12"
					BorderBrush="#22000000"
					BorderThickness="1"
					BoxShadow="0 8 20 0 #12000000"
					Width="260"
					Margin="0,10,0,0">
				<StackPanel Spacing="8">
					<TextBlock Text="Оборудование / Подготовка"
							   FontSize="15"
							   FontWeight="SemiBold"/>

					<TextBlock Text="{Binding RunModeText}"
							   FontSize="12"
							   TextWrapping="Wrap"
							   Opacity="0.85"/>

					<CheckBox Content="Diagnostics mode"
							  IsChecked="{Binding IsDiagnosticsModeEnabled}"
							  IsEnabled="{Binding AreDiagnosticsControlsEnabled}"/>

					<CheckBox Content="Пробовать камеру в diagnostics"
							  IsChecked="{Binding PreferCameraVerificationInDiagnostics}"
							  IsEnabled="{Binding AreDiagnosticsControlsEnabled}"/>

					<TextBlock Text="{Binding EmployeeStatusText}"
							   FontSize="12"
							   TextWrapping="Wrap"/>

					<TextBlock Text="{Binding PreflightSummary}"
							   FontSize="12"
							   FontWeight="SemiBold"
							   TextWrapping="Wrap"/>

					<Button Content="Проверить оборудование"
							Command="{Binding CheckHardwareCommand}"
							HorizontalContentAlignment="Center"/>

					<Button Content="Тестовый прогон"
							Command="{Binding RunTestRunCommand}"
							IsEnabled="{Binding AreDiagnosticsControlsEnabled}"
							HorizontalContentAlignment="Center"/>

					<ScrollViewer MaxHeight="165">
						<ItemsControl ItemsSource="{Binding DeviceStatuses}">
							<ItemsControl.ItemTemplate>
								<DataTemplate x:DataType="devices:DeviceStatusSnapshot">
									<Border BorderBrush="{Binding AccentBrush}"
											BorderThickness="1"
											CornerRadius="10"
											Background="White"
											Padding="8"
											Margin="0,0,0,6">
										<StackPanel Spacing="3">
											<DockPanel>
												<TextBlock DockPanel.Dock="Right"
														   Text="{Binding StateText}"
														   Foreground="{Binding AccentBrush}"
														   FontWeight="SemiBold"
														   HorizontalAlignment="Right"/>
												<TextBlock Text="{Binding DisplayName}"
														   FontWeight="SemiBold"
														   TextWrapping="Wrap"/>
											</DockPanel>

											<TextBlock Text="{Binding Detail}"
													   FontSize="11"
													   TextWrapping="Wrap"
													   Opacity="0.82"/>

											<TextBlock Text="{Binding BlockingText}"
													   FontSize="10"
													   Foreground="{Binding AccentBrush}"
													   Opacity="0.85"/>
										</StackPanel>
									</Border>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</ScrollViewer>

					<TextBlock Text="{Binding TestRunSummary}"
							   FontSize="11"
							   TextWrapping="Wrap"
							   Opacity="0.85"/>

					<ScrollViewer MaxHeight="90">
						<ItemsControl ItemsSource="{Binding LogEntries}">
							<ItemsControl.ItemTemplate>
								<DataTemplate x:DataType="diag:AppLogEntry">
									<TextBlock Text="{Binding Summary}"
											   FontSize="11"
											   TextWrapping="Wrap"
											   Foreground="{Binding AccentBrush}"
											   Margin="0,0,0,4"/>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</ScrollViewer>
				</StackPanel>
			</Border>
		</StackPanel>



		<!-- Сообщения/ошибки: компактная карточка -->
		<Border Grid.Row="2" Grid.Column="0"
				IsVisible="False"
				Background="#FFFFFF"
				CornerRadius="14"
				Padding="0"
				BorderBrush="#22000000"
				BorderThickness="1"
				BoxShadow="0 10 30 0 #22000000"
				Margin="0,0,20,10"
				VerticalAlignment="Top"
				Width="300"
				MaxWidth="300">

			<Grid ColumnDefinitions="8,*" RowDefinitions="Auto">

				<!-- Цветная полоска слева (берёт твой ResultColor) -->
				<Border Grid.Column="0"
						Background="{Binding ResultColor}"
						CornerRadius="14,0,0,14" />

				<!-- Контент -->
				<Border Grid.Column="1" Padding="12,10" Background="Transparent">
					<StackPanel Spacing="6">

						<TextBlock Text="Панель информации"
								   FontSize="12"
								   FontWeight="SemiBold"
								   Opacity="0.7" />

						<!-- Ограничиваем высоту, чтобы не раздувалось -->
						<ScrollViewer MaxHeight="90">
							<TextBlock Text="{Binding ResultMessage}"
									   Foreground="{Binding ResultColor}"
									   FontWeight="SemiBold"
									   FontSize="14"
									   TextWrapping="Wrap" />
						</ScrollViewer>

					</StackPanel>
				</Border>

			</Grid>
		</Border>


		<!-- Камера: адаптивно по высоте -->
		<Border Grid.Row="1" Grid.Column="1"
				Background="#F2F2F2"
				CornerRadius="16"
				BorderBrush="{Binding VerdictColor, FallbackValue=#81C784}"
				BorderThickness="3"
				MaxWidth="560"
				MaxHeight="340"
				MinHeight="200"
				VerticalAlignment="Stretch"
				HorizontalAlignment="Stretch"
				Margin="0,10,0,10">

			<Image Source="{Binding CameraFrame}"
				   Stretch="Uniform" />
		</Border>


		<!-- Окно рекомендаций: адаптивно -->
		<Border Grid.Row="2" Grid.Column="1"
				IsVisible="{Binding IsVerdictVisible}"
				Background="White"
				CornerRadius="20"
				Padding="16"
				BorderBrush="{Binding VerdictColor, FallbackValue=#4CAF50}"
				BorderThickness="3"
				MaxWidth="560"
				MinHeight="110"
				VerticalAlignment="Top"
				HorizontalAlignment="Stretch"
				Margin="0,0,0,10">

			<!-- если текст длинный — будет прокрутка, и блок не раздуется -->
			<StackPanel Spacing="10"
						VerticalAlignment="Center"
						HorizontalAlignment="Center">
				<TextBlock Text="{Binding FinalVerdict}"
						   TextWrapping="Wrap"
						   FontSize="28"
						   FontWeight="SemiBold"
						   TextAlignment="Center"
						   HorizontalAlignment="Center"
						   Foreground="{Binding VerdictColor}" />
				<TextBlock Text="{Binding VerdictReason}"
						   TextWrapping="Wrap"
						   FontSize="18"
						   FontWeight="SemiBold"
						   TextAlignment="Center"
						   HorizontalAlignment="Center"
						   Foreground="Black" />
				<TextBlock Text="{Binding VerdictRecommendation}"
						   TextWrapping="Wrap"
						   FontSize="16"
						   TextAlignment="Center"
						   HorizontalAlignment="Center"
						   Foreground="#333333" />
			</StackPanel>
		</Border>




		<!-- История измерений: Фиксированный размер (не расширяется автоматически), верх выровнен с камерой -->
		<StackPanel Grid.Row="1" Grid.Column="2" Grid.RowSpan="2"
					IsVisible="False"
					Margin="20,0,0,0" VerticalAlignment="Top" MaxHeight="600">

			<TextBlock Text="История измерений" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" />

			<Border Background="#F3F3F3" CornerRadius="16" Padding="12"
					BoxShadow="0 10 30 0 #22000000">
				<ScrollViewer MaxHeight="520">
					<ListBox Classes="history"
								ItemsSource="{Binding Measurements}"
								SelectionMode="Single"
								Focusable="False"
								Background="Transparent"
								BorderThickness="0">

						<ListBox.ItemTemplate>
							<DataTemplate x:DataType="models:VitalMeasurement">
								<Border Background="White" CornerRadius="14" Padding="12"
										BoxShadow="0 6 16 0 #1A000000">

									<Grid ColumnDefinitions="6,*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="10">

										<!-- Полоска статуса слева -->
										<Border Grid.RowSpan="2" Grid.Column="0"
												Background="{Binding Recommendation, Converter={StaticResource RecToBrush}}"
												CornerRadius="10,0,0,10"/>

										<TextBlock Grid.Column="1"
												   Text="{Binding Timestamp, StringFormat='Время: {0:dd.MM.yyyy HH:mm}'}"
												   FontSize="13" FontWeight="SemiBold" />

										<TextBlock Grid.Row="1" Grid.Column="1"
												   Text="{Binding EmployeeId, StringFormat='ID: {0}'}"
												   FontSize="12" Opacity="0.75" />

										<!-- Бейдж справа -->
										<Border Grid.RowSpan="2" Grid.Column="2"
												Background="{Binding Recommendation, Converter={StaticResource RecToBrush}}"
												CornerRadius="999" Padding="10,4"
												VerticalAlignment="Center" MaxWidth="240">
											<TextBlock Text="{Binding Recommendation}"
													   Foreground="White"
													   FontSize="12" FontWeight="SemiBold"
													   TextTrimming="CharacterEllipsis" />
										</Border>

									</Grid>
								</Border>
							</DataTemplate>
						</ListBox.ItemTemplate>

					</ListBox>
				</ScrollViewer>
			</Border>
		</StackPanel>


		<!-- Нижняя зона: подпись + плитки параметров -->
		<StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"
					IsVisible="False"
					HorizontalAlignment="Center"
					VerticalAlignment="Bottom"
					Margin="0,20,0,20"
					Spacing="10"
					MaxWidth="1200">

			<!-- Надпись над плитками -->
			<TextBlock Text="Плитки с данными присутствуют только в первой версии программы для наглядной симуляции работы"
					   FontSize="13"
					   FontWeight="SemiBold"
					   Foreground="#000000"
					   TextAlignment="Center"
					   TextWrapping="Wrap"
					   MaxWidth="900"
					   Margin="0,0,0,6" />

			<!-- Плитки параметров: В линию горизонтально в нижней части (один ряд, 8 колонок) -->
			<Grid ColumnDefinitions="*,*,*,*,*,*,*,*"
				  RowDefinitions="Auto"
				  HorizontalAlignment="Center"
				  VerticalAlignment="Bottom"
				  MaxWidth="1200">

				<!-- Ряд 1: 8 плиток в линию (убрана холестерин) -->
				<Border Grid.Column="0" Background="#E3F2FD" CornerRadius="16" Padding="24" Margin="4">
					<TextBlock Text="{Binding LastHeartRate, StringFormat='ЧСС&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#1565C0" LineHeight="24"/>
				</Border>

				<Border Grid.Column="1" Background="#FFF3E0" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastSaturation, StringFormat='Сатурация&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#EF6C00" LineHeight="24"/>
				</Border>

				<Border Grid.Column="2" Background="#E8F5E9" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastBloodPressure, StringFormat='Давление&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#2E7D32" LineHeight="24"/>
				</Border>

				<Border Grid.Column="3" Background="#FFFDE7" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastTemperature, StringFormat='Температура&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#F57F17" LineHeight="24"/>
				</Border>

				<Border Grid.Column="4" Background="#E1F5FE" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastGlucose, StringFormat='Глюкоза&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#0277BD" LineHeight="24"/>
				</Border>

				<Border Grid.Column="5" Background="#FFEBEE" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastAlcoholLevel, StringFormat='Алкоголь&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#C62828" LineHeight="24"/>
				</Border>

				<Border Grid.Column="6" Background="#E8EAF6" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastActivityLevel, StringFormat='Активность&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#303F9F" LineHeight="24"/>
				</Border>

				<Border Grid.Column="7" Background="#F1F8E9" CornerRadius="16" Padding="24,18,24,18" Margin="4">
					<TextBlock Text="{Binding LastRecommendation, StringFormat='Вердикт&#10;{0}'}"
							   FontSize="12" TextWrapping="Wrap" MaxWidth="120"
							   FontWeight="Bold" TextAlignment="Center"
							   Foreground="#33691E" LineHeight="24"/>
				</Border>

			</Grid>
		</StackPanel>


		<!-- Статусная панель: Внизу по центру -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20" IsVisible="False">
            <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='Дата/время: {0:dd.MM.yyyy HH:mm}'}" FontSize="14" Margin="0,0,20,0" />
            <TextBlock x:Name="StatusText" Text="Статус: Камера и YOLO готовы" FontSize="14" Foreground="#388E3C" />
        </StackPanel>


		<Button Grid.Row="4" Grid.Column="0" Content="Синхронизировать с сайтом" Command="{Binding SyncDataCommand}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,10" />
		
        <!-- Кнопка выхода: Внизу справа, не наезжает -->
		<Button Grid.Row="4" Grid.Column="2" Content="Завершить и выйти" Command="{Binding FinishAndExitCommand}" Width="200" HorizontalContentAlignment="Center" Margin="0,0,10,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="16,8" />

		<TextBlock Grid.Row="4" Grid.Column="0"
				   IsVisible="False"
				   Text="{Binding SyncMessage}"
				   FontSize="11"
				   TextWrapping="Wrap"
				   MaxWidth="320"
				   HorizontalAlignment="Left"
				   VerticalAlignment="Bottom"
				   Margin="10,0,0,44" />
    </Grid>

		<Border IsVisible="{Binding IsDevicePanelOpen}"
				Background="#70000000"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				ZIndex="20">
			<Grid HorizontalAlignment="Stretch"
				  VerticalAlignment="Stretch"
				  Margin="32">
				<Border Background="#FFFDF9"
						BorderBrush="#22000000"
						BorderThickness="1"
						CornerRadius="20"
						BoxShadow="0 24 56 0 #30000000"
						Padding="24,22"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"
						Width="820"
						MaxWidth="900"
						MaxHeight="920"
						MinHeight="520">
					<Grid RowDefinitions="Auto,Auto,*,Auto"
						  RowSpacing="16">
						<Grid ColumnDefinitions="*,Auto"
							  ColumnSpacing="12">
							<StackPanel Spacing="4">
								<TextBlock Text="Оборудование / Подготовка"
										   FontSize="22"
										   FontWeight="SemiBold"
										   Foreground="#1E1E1E"/>
								<TextBlock Text="{Binding DevicePanelSummary}"
										   FontSize="12"
										   TextWrapping="Wrap"
										   Opacity="0.85"/>
							</StackPanel>

							<Button Grid.Column="1"
									Content="Закрыть"
									Command="{Binding CloseDevicePanelCommand}"
									HorizontalAlignment="Right"
									VerticalAlignment="Top"/>
						</Grid>

						<Border Grid.Row="1"
								Background="#FFF4E8"
								CornerRadius="12"
								Padding="12"
								BorderBrush="#12000000"
								BorderThickness="1">
							<StackPanel Spacing="8">
								<TextBlock Text="{Binding StartBlockingSummary}"
										   FontSize="13"
										   FontWeight="SemiBold"
										   TextWrapping="Wrap"
										   Foreground="#8A2C0A"/>
								<TextBlock Text="{Binding RunModeText}"
										   FontSize="12"
										   TextWrapping="Wrap"/>
								<CheckBox Content="Diagnostics mode"
										  IsChecked="{Binding IsDiagnosticsModeEnabled}"
										  IsEnabled="{Binding AreDiagnosticsControlsEnabled}"/>
								<CheckBox Content="Пробовать камеру в diagnostics"
										  IsChecked="{Binding PreferCameraVerificationInDiagnostics}"
										  IsEnabled="{Binding AreDiagnosticsControlsEnabled}"/>
								<TextBlock Text="{Binding EmployeeStatusText}"
										   FontSize="12"
										   TextWrapping="Wrap"/>
								<TextBlock Text="{Binding PreflightSummary}"
										   FontSize="12"
										   FontWeight="SemiBold"
										   TextWrapping="Wrap"/>
								<TextBlock Text="IP сервера сайта"
										   FontSize="12"
										   FontWeight="SemiBold"
										   Margin="0,4,0,0"/>
								<Grid ColumnDefinitions="*,Auto"
									  ColumnSpacing="8">
									<TextBox Text="{Binding ServerIp}"
											 Watermark="РќР°РїСЂРёРјРµСЂ, 192.168.1.10"/>
									<Button Grid.Column="1"
											Content="Применить"
											Command="{Binding ApplyServerIpCommand}"
											Padding="12,6"
											HorizontalAlignment="Right"/>
								</Grid>
								<TextBox IsVisible="False" Text="{Binding ServerIp}"
										 Watermark="Например, 192.168.1.10"/>
								<TextBlock Text="{Binding SyncEndpointUrl, StringFormat='URL sync: {0}'}"
										   FontSize="11"
										   TextWrapping="Wrap"
										   Opacity="0.85"/>
								<TextBlock Text="{Binding SyncMessage}"
										   FontSize="11"
										   TextWrapping="Wrap"
										   Foreground="#8A2C0A"/>
							</StackPanel>
						</Border>

						<ScrollViewer Grid.Row="2"
									  VerticalScrollBarVisibility="Visible"
									  HorizontalScrollBarVisibility="Disabled"
									  AllowAutoHide="False">
							<StackPanel Spacing="12">
								<Grid ColumnDefinitions="*,*"
									  ColumnSpacing="10">
									<Button Content="Проверить оборудование"
											Command="{Binding CheckHardwareCommand}"
											HorizontalContentAlignment="Center"/>
									<Button Grid.Column="1"
											Content="Тестовый прогон"
											Command="{Binding RunTestRunCommand}"
											IsEnabled="{Binding AreDiagnosticsControlsEnabled}"
											HorizontalContentAlignment="Center"/>
								</Grid>

								<Border Background="#FFFFFF"
										CornerRadius="12"
										Padding="12"
										BorderBrush="#15000000"
										BorderThickness="1">
									<StackPanel Spacing="8">
										<TextBlock Text="Статусы оборудования"
												   FontSize="12"
												   FontWeight="SemiBold"/>
										<ItemsControl ItemsSource="{Binding DeviceStatuses}">
											<ItemsControl.ItemTemplate>
												<DataTemplate x:DataType="devices:DeviceStatusSnapshot">
													<Border BorderBrush="{Binding AccentBrush}"
															BorderThickness="1"
															CornerRadius="12"
															Background="White"
															Padding="10"
															Margin="0,0,0,8">
														<StackPanel Spacing="4">
															<DockPanel>
																<TextBlock DockPanel.Dock="Right"
																		   Text="{Binding StateText}"
																		   Foreground="{Binding AccentBrush}"
																		   FontWeight="SemiBold"
																		   HorizontalAlignment="Right"/>
																<TextBlock Text="{Binding DisplayName}"
																		   FontWeight="SemiBold"
																		   TextWrapping="Wrap"/>
															</DockPanel>

															<TextBlock Text="{Binding Detail}"
																	   FontSize="11"
																	   TextWrapping="Wrap"
																	   Opacity="0.82"/>

															<TextBlock Text="{Binding BlockingText}"
																	   FontSize="10"
																	   Foreground="{Binding AccentBrush}"
																	   Opacity="0.85"/>
														</StackPanel>
													</Border>
												</DataTemplate>
											</ItemsControl.ItemTemplate>
										</ItemsControl>
									</StackPanel>
								</Border>

								<Border Background="#FFFFFF"
										CornerRadius="12"
										Padding="12"
										BorderBrush="#15000000"
										BorderThickness="1">
									<StackPanel Spacing="6">
										<TextBlock Text="Последний тестовый прогон"
												   FontSize="12"
												   FontWeight="SemiBold"/>
										<TextBlock Text="{Binding TestRunSummary}"
												   FontSize="11"
												   TextWrapping="Wrap"
												   Opacity="0.85"/>
									</StackPanel>
								</Border>

								<Border Background="#FFFFFF"
										CornerRadius="12"
										Padding="12"
										BorderBrush="#15000000"
										BorderThickness="1">
									<StackPanel Spacing="8">
										<TextBlock Text="Логи"
												   FontSize="12"
												   FontWeight="SemiBold"/>
										<ItemsControl ItemsSource="{Binding LogEntries}">
											<ItemsControl.ItemTemplate>
												<DataTemplate x:DataType="diag:AppLogEntry">
													<TextBlock Text="{Binding Summary}"
															   FontSize="11"
															   TextWrapping="Wrap"
															   Foreground="{Binding AccentBrush}"
															   Margin="0,0,0,4"/>
												</DataTemplate>
											</ItemsControl.ItemTemplate>
										</ItemsControl>
									</StackPanel>
								</Border>
							</StackPanel>
						</ScrollViewer>

						<TextBlock Grid.Row="3"
								   Text="Основной экран остаётся чистым, а детали подготовки доступны по кнопке в шапке."
								   FontSize="11"
								   TextWrapping="Wrap"
								   Opacity="0.72"/>
					</Grid>
				</Border>
			</Grid>
		</Border>

		<Border x:Name="Overlay"
        Background="#80000000"
        IsVisible="{Binding IsOverlayVisible}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        IsHitTestVisible="True"
        ZIndex="30">
			
			

			<StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">

				<!-- ===================== CAPTCHA (твоя старая логика, только UI стабилизируем) ===================== -->
				<Viewbox IsVisible="{Binding IsCaptchaVisible}"
						 Stretch="Uniform"
						 StretchDirection="DownOnly"
						 HorizontalAlignment="Center"
						 VerticalAlignment="Center"
						 Margin="20">

					<Border Background="White"
							CornerRadius="8"
							Padding="20"
							Width="450"
							BorderBrush="#22000000"
							BorderThickness="1"
							BoxShadow="0 10 30 0 #22000000">

						<StackPanel Spacing="10">

							<!-- Главное: явный чёрный цвет, чтобы текст не исчезал на Win11/темной теме -->
							<TextBlock Text="{Binding CaptchaPrompt}"
									   FontSize="18"
									   FontWeight="Bold"
									   TextAlignment="Center"
									   TextWrapping="Wrap"
									   Foreground="#000000"/>

							<TextBlock Text="{Binding SelectedCount, StringFormat='Выбрано: {0} элементов'}"
									   FontSize="14"
									   TextAlignment="Center"
									   TextWrapping="Wrap"
									   Foreground="#1A1A1A"/>

							<Canvas Height="200" Margin="10">
								<Button Canvas.Left="{Binding CaptchaLefts[0]}" Canvas.Top="{Binding CaptchaTops[0]}"
										Content="{Binding CaptchaItems[0]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="0"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[0]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[1]}" Canvas.Top="{Binding CaptchaTops[1]}"
										Content="{Binding CaptchaItems[1]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="1"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[1]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[2]}" Canvas.Top="{Binding CaptchaTops[2]}"
										Content="{Binding CaptchaItems[2]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="2"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[2]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[3]}" Canvas.Top="{Binding CaptchaTops[3]}"
										Content="{Binding CaptchaItems[3]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="3"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[3]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[4]}" Canvas.Top="{Binding CaptchaTops[4]}"
										Content="{Binding CaptchaItems[4]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="4"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[4]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[5]}" Canvas.Top="{Binding CaptchaTops[5]}"
										Content="{Binding CaptchaItems[5]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="5"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[5]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[6]}" Canvas.Top="{Binding CaptchaTops[6]}"
										Content="{Binding CaptchaItems[6]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="6"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[6]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[7]}" Canvas.Top="{Binding CaptchaTops[7]}"
										Content="{Binding CaptchaItems[7]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="7"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[7]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
								<Button Canvas.Left="{Binding CaptchaLefts[8]}" Canvas.Top="{Binding CaptchaTops[8]}"
										Content="{Binding CaptchaItems[8]}"
										Command="{Binding CaptchaClickCommand}" CommandParameter="8"
										Width="50" Height="50" FontSize="20"
										Background="{Binding CaptchaBackgrounds[8]}" Foreground="Black"
										BorderBrush="Gray" BorderThickness="1" CornerRadius="25" />
							</Canvas>

							<Button Content="Принять"
									Command="{Binding CheckCaptchaCommand}"
									HorizontalAlignment="Center"
									Width="120"/>

							<TextBlock Text="{Binding CaptchaMessage}"
									   FontSize="14"
									   TextAlignment="Center"
									   TextWrapping="Wrap"
									   Foreground="#C62828"/>
						</StackPanel>
					</Border>
				</Viewbox>


				<!-- ===================== Панель инструкций (как у тебя, просто центрируем) ===================== -->
				<Border IsVisible="{Binding !IsCaptchaVisible}"
						Background="White"
						CornerRadius="8"
						Padding="20"
						Margin="20"
						Width="300"
						HorizontalAlignment="Center"
						VerticalAlignment="Center">

					<StackPanel Spacing="10">
						<TextBlock Text="{Binding CurrentInstruction}"
								   FontSize="16"
								   FontWeight="Bold"
								   TextAlignment="Center"
								   TextWrapping="Wrap"
								   Foreground="#000000"/>

						<TextBlock Text="{Binding PressurePrepCountdownText}"
								   IsVisible="{Binding IsPressurePrepActive}"
								   FontSize="13"
								   TextAlignment="Center"
								   TextWrapping="Wrap"
								   Foreground="#5A5A5A"/>

						<Button Content="Старт"
								Command="{Binding StartPressurePrepCommand}"
								IsVisible="{Binding IsPressurePrepActive}"
								HorizontalAlignment="Center"
								Width="160"
								Background="#2E7D32"/>

						<ProgressBar Value="{Binding ProgressValue}"
									 Minimum="0"
									 Maximum="100"
									 Height="20"
									 Margin="10">
							<ProgressBar.Foreground>
								<LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
									<GradientStop Color="#FF6D00" Offset="0"/>
									<GradientStop Color="#FFCC40" Offset="1"/>
								</LinearGradientBrush>
							</ProgressBar.Foreground>
						</ProgressBar>

						<Button Content="Экстренная остановка"
								Command="{Binding EmergencyStopCommand}"
								IsEnabled="{Binding CanEmergencyStop}"
								HorizontalAlignment="Center"
								Width="220"
								Background="#B71C1C"/>

						<TextBlock Text="{Binding HardwareSafetyStatus}"
								   FontSize="13"
								   FontWeight="SemiBold"
								   TextAlignment="Center"
								   TextWrapping="Wrap"
								   Foreground="#7A1F1F"/>
					</StackPanel>
				</Border>


			</StackPanel>
		</Border>
    </Grid>
</Window>
